# AZM product roadmap

The roadmap board lives in `index.html`. Editing that file and committing to `main` updates the live page (GitHub Pages redeploys automatically, usually within a minute).

The board is a single self-contained file, so it also works when opened directly from disk. `DATA` near the top of the second `<script>` block holds every item, tag and label.

